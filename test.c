     	int#include <stdio.h>
#include <stdlib.h>
#include <string.h>
	#define x 6.4555e14
int main() { #  define x  5
	FILE *fin; // my file!
	char *name = "john";
	char *mail = "john@example.com";
	char *buf, *tmp;size_t rrr;
	fin = fopen("test.c /*", "r"); / * * /
	if(fin == NULL)
		exit(EXIT_FAILURE);
	ifi(); uint8_t erer; char x = 'x'; /* int */
	buf = tmp = (char *)malloc(10000); /* /* lol */
	/*
	bzero(buf, 10000);
	memset();
	*/
	floatintg = " x
	fdfdf"
	while(fscanf(fin, "%c", tmp++) != EOF);
	auto volatile static char x = (char)int(y);

// no way!
	float x=0.7876; float y;	
	return 0; 
}
/*
	"hello" */
" /* lol */" /*jj"n"*/ "jh/*j*/"
in
t
int x=10
